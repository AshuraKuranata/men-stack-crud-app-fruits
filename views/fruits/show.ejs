<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= fruit.name%></title>
</head>
<body>
    <%- include('../partials/nav.ejs') %>
    
    <h1><%= fruit.name%></h1>
    <p>You currently have <%= fruit.quantity %>.</p>
    <% if (fruit.isReadyToEat) { %>
        <p>This fruit is ready to eat!</p>
    <% } else { %>
        <p>This fruit is not ready to eat!</p>
    <% } %>
    <form action="/fruits/<%=fruit._id%>?_method=DELETE" method="POST">
        <button type="submit">Delete <%= fruit.name%></button>
    </form>
    <a href="/fruits/<%= fruit._id%>/edit">Edit <%= fruit.name %></a>

</body>
</html>



<!-- Functionality to add name next to quantity and a general form of plurality
<% if (fruit.quantity === "1") { %>
    <p> You currently have <%= fruit.quantity %> <%=fruit.name%>. </p>
<% } else {%>
    <p> You currently have <%= fruit.quantity %> <%=fruit.name%>s. </p>
<% } %> -->